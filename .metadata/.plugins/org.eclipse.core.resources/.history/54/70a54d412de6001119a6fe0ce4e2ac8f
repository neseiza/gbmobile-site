package gbmobile

import webpage.Label
import webpage.Section

class LabelTagLib {
	def label = {
		attrs, body ->		
		Integer id = Integer.parseInt(attrs.sectionID)
		sec = Section.getAt("id")
		
		//Label label = Section.countByLabelAnd(id)
		//Label label = Section.findByLabel(id)
		
		println ('Section: ' + sec)
	}
}
